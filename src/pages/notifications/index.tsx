import React from 'react';
import { Layout, NotificationsTable } from '../../components';

const Notifications = () => {
	return (
		<Layout>
			<NotificationsTable />
		</Layout>
	);
};

export default Notifications;
