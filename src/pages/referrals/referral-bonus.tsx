import React from 'react';
import { Layout, ReferralBonusTable } from '../../components';

const ReferralBonus = () => {
	return (
		<Layout>
			<ReferralBonusTable />
		</Layout>
	);
};

export default ReferralBonus;
